-- lua/config/agent.lua — opencode.nvim AI Agent 集成

-- opencode.nvim 无需 setup()，直接使用 API
-- 快捷键已在 keymaps.lua 中定义：
--   <C-a>  Ask（当前选择/行）
--   <C-x>  Select（命令菜单）
--   go     Operator（范围发送）
--
-- blink.cmp 自动检测 opencode.nvim 输入框并提供补全
