# ── 概览模式 (窗口总览) ──
hotarea_size=10          # 触发热区大小(像素)
enable_hotarea=1         # 启用屏幕边缘热区
ov_tab_mode=0            # 标签页模式 (0=平铺, 1=标签)
overviewgappi=5          # 概览内间距(像素)
overviewgappo=30         # 概览外间距(像素)
