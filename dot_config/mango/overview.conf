# ── 概览模式 (窗口总览) ──
# 触发热区大小(像素)
hotarea_size=10
# 启用屏幕边缘热区
enable_hotarea=1
# 概览内间距(像素)
overviewgappi=5
# 概览外间距(像素)
overviewgappo=30
# 0.17.0: 全屏时禁用热区触发 (避免全屏游戏/视频被边缘热区误触)
hotarea_disable_on_fullscreen=1
